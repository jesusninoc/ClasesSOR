# Instalaciones

## Instalar Ubuntu LTS
* https://ubuntu.com/download/server/thank-you?version=25.04&architecture=amd64
* https://hr.releases.ubuntu.com/24.04/ubuntu-24.04.3-live-server-amd64.iso

## Instalar Windows Server 2022
* https://www.microsoft.com/es-es/evalcenter/download-windows-server-2022

## Tareas de las instalaciones

- SO de red
- Cliente/servidor
- CPU, RAM, disco y red
- GPT
- Particiones
- Sistemas de archivos
- Montaje
- `/etc/fstab`
- Servicios
- SSH
- `systemctl`
- Superficie mínima
- Dimensionamiento
- Migración y copia de seguridad

------------
------------

# Instalación del servidor

## 2.1. Planificación e instalación

### Planificar antes de instalar

Definir previamente:

- **Hardware / VM:**
  - CPU.
  - RAM.
  - Disco.
  - En laboratorio, normalmente máquina virtual.
  - En producción, puede existir RAID para aumentar la fiabilidad.

- **Almacenamiento:**
  - Planificar particiones.
  - Separar sistema y datos.
  - Elegir sistema de archivos.

- **Identificación:**
  - Nombre definitivo del servidor.
  - Sin espacios ni acentos.
  - Ejemplo: `SRV-DATOS`.

- **Red:**
  - IP estática.
  - Máscara/prefijo.
  - Puerta de enlace.
  - DNS.

- **Función del servidor:**
  - Archivos.
  - Dominio.
  - Servicios de red.
  - Otros servicios.

> **Remarcar:** primero se decide qué servidor necesitamos y cómo debe quedar; después se instala.

### Windows Server

Proceso general:

1. Arrancar desde el medio de instalación.
2. Elegir edición.
3. Elegir instalación con o sin interfaz gráfica.
4. Seleccionar disco/particiones.
5. Completar la instalación.
6. Configurar contraseña de administrador.
7. Configurar nombre y red.

### Linux Server

- Instalar una distribución:
  - Ubuntu Server.
  - Debian.
  - etc.
- Habitualmente sin entorno gráfico.
- Configurar:
  - usuario;
  - particiones;
  - red.

Comprobación básica (si hay DHCP activo):

```bash
ip address show
ping -c 2 8.8.8.8
```

> **Remarcar:** tanto Windows Server como Linux Server deben practicarse en el laboratorio virtualizado.

> **Idea clave:** saber planificar e instalar un SO de red propietario y otro libre.

---

## 2.2. Configuración inicial y comprobación

Una instalación terminada **no significa que el servidor esté listo**.

### Configuración inicial

Comprobar y configurar:

- **Red:**
  - IP estática.
  - Puerta de enlace.
  - DNS.

- **Nombre definitivo** del equipo.
- **Actualizaciones de seguridad.**
- Zona horaria.
- Idioma y teclado.
- Contraseña fuerte de administrador.
- Cortafuegos.
- Deshabilitar servicios innecesarios.
- Instalar los roles/servicios necesarios.

En Linux:

```bash
sudo apt update
sudo apt upgrade -y
```

### Comprobación

Verificar:

- El servidor arranca correctamente.
- El sistema es estable.
- Tiene conectividad.
- Resuelve nombres DNS.
- Los clientes pueden alcanzarlo.
- Los servicios instalados responden.

### Documentación

Registrar:

- Configuración del servidor.
- Nombre.
- IP.
- Roles y servicios.
- Cambios realizados.
- Información de acceso de forma segura.

> **Remarcar:** configurar, comprobar y documentar forman parte de la instalación.

> **Pregunta:** ¿qué sentido tiene instalar un servidor si después no comprobamos que los clientes pueden acceder a él?

---

## 2.3. Automatización de las instalaciones

### ¿Por qué automatizar?

Instalar manualmente:

- válido para uno o pocos servidores;
- lento si hay muchos equipos;
- puede provocar errores humanos.

Tres métodos principales:

### 1. Instalación desatendida

- El instalador recibe las respuestas automáticamente.
- Se define previamente:
  - idioma;
  - particiones;
  - red;
  - nombre;
  - usuarios;
  - componentes.

En Windows:

```text
unattend.xml
```

### 2. Clonado mediante imagen

- Se prepara un equipo modelo.
- Se instala y configura.
- Se captura como imagen.
- Se replica en otros equipos.

> **Remarcar:** no se configura veinte veces lo mismo; se prepara una vez y se replica.

### 3. Instalación por red

El equipo arranca desde la tarjeta de red y obtiene el instalador desde la red.

Intervienen normalmente:

- **DHCP:** proporciona configuración inicial y ayuda a localizar el servidor de arranque.
- **TFTP:** proporciona los archivos iniciales de arranque.
- **Servidor de instalación:** proporciona el resto de archivos.

> **Remarcar:** DHCP, TFTP y servidor de instalación cumplen funciones diferentes.

### Sysprep en Windows

Antes de clonar una instalación Windows:

```cmd
C:\Windows\System32\Sysprep\sysprep.exe /generalize /oobe /shutdown
```

- `generalize`: elimina información específica del equipo.
- `oobe`: prepara el primer arranque.
- `shutdown`: apaga el equipo.

### Laboratorio virtualizado

Una alternativa muy práctica:

- Crear una **plantilla de máquina virtual**.
- Clonar la plantilla.
- Modificar nombre, IP y configuración específica.

> **Pregunta:** ¿qué método utilizarías para instalar 1 servidor? ¿Y 30 servidores iguales?

---

## 2.4. Fichero de respuestas y comprobación de la instalación automatizada

### ¿Qué contiene un fichero de respuestas?

Información que normalmente preguntaría el instalador:

- Idioma.
- Teclado.
- Zona horaria.
- País.
- Particionado.
- Sistemas de archivos.
- Nombre del equipo.
- Dominio.
- Usuarios.
- Contraseñas iniciales.
- Configuración de red.
- Roles y paquetes.
- Acciones posteriores a la instalación.

### Punto especialmente delicado

**Particionado:**

- Disco utilizado.
- Particiones.
- Tamaños.
- Sistemas de archivos.
- Qué hacer con los datos existentes.

> **PELIGRO:** un error en el fichero puede borrar un disco equivocado.

### Probar antes de producción

La instalación automatizada debe probarse:

1. En una máquina virtual.
2. Con disco vacío.
3. En la red de laboratorio.
4. Comprobando que no solicita intervención.
5. Repitiendo la prueba hasta obtener un resultado correcto.

> **Remarcar:** escribir el fichero es solo la mitad del trabajo; la otra mitad es probarlo.

### Comprobación posterior

Después de una instalación desatendida comprobar:

- Nombre correcto.
- Particionado correcto.
- Sistemas de archivos correctos.
- IP y red correctas.
- No existe una IP duplicada.
- Componentes previstos instalados.
- Componentes innecesarios no instalados.
- Cuentas correctas.
- Cambio obligatorio de contraseña inicial.
- Sistema actualizado.
- Hora sincronizada.

### Versionado

Guardar las versiones del fichero de respuestas:

- Fecha.
- Cambios realizados.
- Motivo del cambio.
- Versión que funcionó.

> **Idea clave:** el fichero de respuestas también es documentación técnica y debe controlarse por versiones.

---

## 2.5. Preferencias del entorno y actualización del sistema

### Configuración del entorno

Configurar y documentar:

- **Nombre del equipo:**
  - corto;
  - descriptivo;
  - sin espacios ni acentos.

Ejemplo:

```text
SRV-DATOS
```

- **Zona horaria y hora.**
- **Sincronización horaria.**
- Idioma.
- Distribución de teclado.
- Cuenta del administrador.

Linux:

```bash
sudo hostnamectl set-hostname srv-datos
sudo timedatectl set-timezone Europe/Madrid
timedatectl status
sudo localectl set-keymap es
```

Windows:

```powershell
Rename-Computer -NewName SRV-DATOS -Restart
Set-TimeZone -Name "Romance Standard Time"
```

> **Remarcar:** la hora correcta es importante porque afecta a registros, autenticación y diagnóstico.

### Actualización del servidor

Actualizar un servidor requiere planificación.

**Antes:**

- Copia de seguridad comprobada o instantánea.
- Avisar de la ventana de mantenimiento.
- Preferiblemente fuera del horario de servicio.

**Durante:**

- Aplicar actualizaciones.
- Priorizar las de seguridad.
- Registrar los cambios.
- Reiniciar si es necesario.

**Después:**

- Comprobar que arranca.
- Comprobar servicios.
- Revisar registros.
- Documentar el resultado.

**Si falla:**

- Desinstalar la actualización problemática.
- Restaurar la instantánea/copia según el caso.

> **REGLA DE ORO:** la copia de seguridad se hace **antes** de actualizar, no después.

Linux:

```bash
sudo apt update
sudo apt full-upgrade
sudo apt list --upgradable
systemctl list-units --type=service --state=running
```

### Actualizaciones centralizadas

En organizaciones grandes:

- No actualizar servidor por servidor manualmente.
- Utilizar un sistema/repositorio interno.
- Controlar qué actualizaciones se aprueban y cuándo.
- Probar primero en laboratorio.

> **Idea clave:** actualizar no es simplemente pulsar "Actualizar"; en producción es un cambio controlado.

---

## 2.6. Preferencias del entorno personal del administrador

### Principio general

> **Personalizar para administrar, no para decorar.**

En un servidor evitar:

- Fondos innecesarios.
- Salvapantallas.
- Programas personales.
- Software que no aporta nada a la administración.

Porque:

- consume recursos;
- aumenta la superficie de ataque;
- complica el mantenimiento.

### Cuenta del administrador

Cada técnico debería utilizar:

- Su propia cuenta nominal.
- Sus propios privilegios.
- Identificación individual en los registros.

Evitar compartir permanentemente una cuenta de administrador.

> **Remarcar:** si todos utilizan la misma cuenta, después es difícil saber quién realizó un cambio.

### Entorno de línea de comandos

En Linux, se puede personalizar `~/.bashrc`:

```bash
alias ll='ls -lh --color=auto'
export PS1='\u@\h:\w\$ '
source ~/.bashrc
```

El indicador puede mostrar:

```text
usuario@servidor:ruta
```

Esto ayuda a evitar trabajar accidentalmente sobre el servidor equivocado.

### Administración remota

Preparar:

- Herramientas de administración remota.
- Conexión desde el equipo del técnico.
- Variables de entorno y rutas necesarias.
- Configuración regional y teclado.

> **Consejo práctico:** en un laboratorio con varios servidores, que el nombre del equipo aparezca siempre en la consola evita errores.

---

## 2.7. Ejemplo resuelto: instalar y documentar los servidores

### Situación

Hay que instalar:

- Windows Server.
- Linux Server.

Ambos se utilizarán para las siguientes unidades.

### Estrategia

1. Crear las máquinas virtuales.
2. Asignar recursos.
3. Instalar Windows Server.
4. Instalar Linux Server.
5. Configurar:
   - IP estática;
   - nombre;
   - actualizaciones;
   - contraseña de administrador;
   - cortafuegos.
6. Comprobar:
   - arranque;
   - red;
   - DNS;
   - acceso desde los clientes.
7. Documentar la configuración.

### Resultado

Los dos servidores quedan:

- instalados;
- configurados;
- actualizados;
- comprobados;
- documentados.

Y preparados para instalar posteriormente:

- usuarios;
- dominios;
- recursos;
- servicios.

### En la práctica

- Asignar una **IP estática**.
- Actualizar desde el principio.
- Utilizar una contraseña fuerte.
- Activar/configurar el cortafuegos.
- Comprobar conectividad.
- Comprobar DNS.
- Verificar el acceso desde los clientes.
- Documentar toda la configuración.

> **Error frecuente:** pensar que un servidor recién instalado ya está preparado para producción.

> **Corrección:** después de instalar hay que **configurar, actualizar, asegurar, comprobar y documentar**.

---

# Resumen de la unidad

## Conceptos que deben quedar claros

1. **Planificación:** definir recursos, almacenamiento, nombre, red y función antes de instalar.
2. **Instalación:** conocer el proceso tanto en Windows Server como en Linux Server.
3. **Configuración inicial:** IP, DNS, nombre, hora, seguridad y actualizaciones.
4. **Comprobación:** verificar que el servidor funciona y es accesible desde los clientes.
5. **Automatización:** desatendida, clonación e instalación por red.
6. **Fichero de respuestas:** automatiza las decisiones que normalmente realiza el instalador.
7. **Actualizaciones:** deben planificarse y probarse antes de aplicarlas en producción.
8. **Administración:** personalizar el entorno para trabajar mejor, no para decorar.
9. **Documentación:** registrar la configuración y los cambios.
10. **Vía de vuelta:** disponer de copia o instantánea antes de cambios importantes.

## Preguntas finales para la clase

- ¿Qué debemos decidir antes de instalar un servidor?
- ¿Por qué un servidor debería utilizar una IP estática?
- ¿Qué diferencias hay entre instalar Windows Server y Linux Server?
- ¿Por qué muchos servidores Linux no utilizan interfaz gráfica?
- ¿Qué ventajas aporta automatizar una instalación?
- ¿Qué diferencia hay entre instalación desatendida, clonación e instalación por red?
- ¿Qué funciones realizan DHCP y TFTP en una instalación por red?
- ¿Por qué hay que probar un fichero de respuestas en una máquina virtual?
- ¿Qué debemos comprobar después de una instalación automatizada?
- ¿Por qué hay que hacer una copia de seguridad antes de actualizar?
- ¿Por qué es importante sincronizar la hora del servidor?
- ¿Por qué cada administrador debería tener su propia cuenta?
- ¿Qué ventaja tiene mostrar el nombre del servidor en el prompt?
- ¿Qué elementos debemos documentar después de instalar un servidor?

## Idea final

> **Instalar un servidor no es simplemente instalar el sistema operativo: es dejarlo identificado, configurado, actualizado, seguro, comprobado y documentado.**

------------
------------

# Comprobación, virtualización y documentación del servidor

## 3.1. Comprobación de la conectividad con los equipos cliente

### Idea principal

> **Un servidor no se da por bueno hasta que los clientes pueden llegar a él.**

La comprobación debe hacerse **de lo más básico a lo más específico**.

Orden recomendado:

1. Configuración local del servidor.
2. Conectividad del propio servidor.
3. Conectividad cliente → servidor por IP.
4. Resolución de nombres.
5. Puerto y servicio.

> **Remarcar:** si seguimos el orden, el primer fallo nos indica dónde buscar el problema.

### 1. Configuración local

Comprobar:

- IP.
- Máscara/prefijo.
- Puerta de enlace.
- DNS.
- Interfaz de red activa.

Linux:

```bash
ip -br address
ip route
```

### 2. Conectividad básica

Desde el servidor:

```bash
ping -c 4 192.0.2.1
```

- Si funciona → la conectividad básica es correcta.
- Si falla → revisar red física/virtual, configuración y máscara.

### 3. Cliente → servidor por IP

Desde el cliente:

```bash
ping 192.0.2.10
```

- Si funciona → existe conectividad IP.
- Si falla → todavía no tiene sentido revisar DNS o permisos.

### 4. Resolución de nombres

Windows:

```powershell
nslookup srv-datos
```

- Si funciona por IP pero no por nombre → problema de DNS/resolución de nombres.

### 5. Puerto del servicio

Linux:

```bash
ss -tulpn
```

Permite comprobar:
- qué puertos están escuchando;
- qué proceso los utiliza.

Windows:

```powershell
Test-NetConnection 192.0.2.10 -Port 445
```

> **Remarcar:** aquí aparece una causa muy habitual: el servicio funciona, pero el cortafuegos bloquea el puerto.

### Causas más frecuentes

Si un cliente no llega al servidor, revisar:

1. **Modo de red de la máquina virtual.**
2. **IP o máscara incorrectas.**
3. **Cortafuegos bloqueando el servicio.**
4. **Servicio detenido.**

> **REGLA DE DIAGNÓSTICO:** no cambiar cosas al azar. Comprobar, localizar el primer fallo y corregirlo.

---

## 3.2. Instalación en máquinas virtuales

### Ventajas de virtualizar

Permite:

- Tener servidor y clientes en un único equipo.
- Crear laboratorios completos.
- Probar configuraciones.
- Romper una instalación y repetirla.
- Utilizar instantáneas para volver atrás.

> **Idea clave:** la virtualización permite practicar escenarios reales sin necesidad de varios equipos físicos.

### Recursos virtuales

Asignar de forma razonable:

- CPU.
- RAM.
- Disco.
- Tarjetas de red.

> **Remarcar:** asignar todos los recursos al servidor virtual puede dejar al equipo anfitrión sin recursos y hacer que todo funcione peor.

### Disco virtual

**Tamaño fijo**
- Reserva el espacio desde el principio.
- Puede ofrecer un rendimiento más estable.
- Consume inmediatamente el espacio del almacenamiento real.

**Expansión dinámica**
- Crece según se necesita.
- Aprovecha mejor el espacio inicialmente.
- Puede terminar llenando el disco físico.

### Modos de red

**NAT**
- La máquina virtual puede salir a Internet.
- Los equipos externos normalmente no pueden iniciar conexiones hacia ella.
- No es el modo adecuado para un servidor que deben alcanzar otros equipos.

**Puente**
- La máquina virtual aparece en la red como otro equipo.
- Tiene su propia dirección IP.
- Los clientes pueden acceder directamente a ella.

> **Remarcar:** para un servidor que debe ser accesible desde otros equipos de la red, normalmente utilizaremos **modo puente**.

**Solo anfitrión**
- Crea una red aislada entre el anfitrión y las máquinas virtuales.
- Muy útil para laboratorios aislados.

### Complementos de la virtualización

Instalar las herramientas/controladores del programa de virtualización cuando proceda:

- Mejoran compatibilidad y rendimiento.
- Facilitan determinadas funciones entre anfitrión e invitado.

### Orden de arranque

Durante la instalación:

- Arrancar desde la ISO.

Después:

- Retirar la ISO o cambiar el orden de arranque.

> **Error frecuente:** dejar la ISO montada y volver a arrancar accidentalmente el instalador.

### Instantáneas

Una instantánea:

- Guarda el estado de una máquina virtual.
- Permite volver a un estado anterior.
- Es muy útil antes de una práctica o actualización.

Pero:

- **No es una copia de seguridad.**
- Está en el mismo almacenamiento.
- Puede afectar al rendimiento.
- No protege frente a un fallo del disco físico.

> **REGLA DE ORO:** utilizar instantáneas para volver atrás durante una práctica o cambio controlado; utilizar copias de seguridad para proteger los datos.

---

## 3.3. Documentación de la instalación y parte de incidencias

### Idea principal

> **La instalación no termina cuando el servidor arranca; termina cuando queda documentada.**

La documentación permite:

- Que otro técnico pueda hacerse cargo.
- Reconstruir el servidor.
- Conocer las decisiones tomadas.
- Facilitar el mantenimiento.
- Resolver incidencias futuras.

### Ficha de instalación

Debe recoger como mínimo:

| Apartado | Información |
|---|---|
| Identificación | Nombre, ubicación/anfitrión, fecha y técnico |
| Recursos | CPU, RAM, discos y tarjetas de red |
| Sistema operativo | SO, edición, tipo y modo de instalación |
| Particionado | Particiones, tamaños y sistemas de archivos |
| Red | IP, máscara, puerta de enlace y DNS |
| Roles/paquetes | Instalados y motivo |
| Cuentas | Cuentas y política de contraseñas |
| Licencias | Activación cuando sea necesaria |
| Comprobaciones | Pruebas realizadas y resultado |

> **Remarcar:** las contraseñas **no deben escribirse en la ficha**. Se guardan en el sistema seguro de gestión de credenciales correspondiente.

### Parte de incidencias

Registrar:

- Fecha y hora.
- Síntoma exacto.
- Mensaje de error literal.
- Causa identificada.
- Solución aplicada.
- Tiempo empleado.

> **Remarcar:** no escribir "daba error"; escribir exactamente qué ocurrió.

### Incidencias frecuentes

| Problema | Causa habitual | Solución |
|---|---|---|
| No arranca desde la ISO | Orden de arranque o modo UEFI/BIOS incorrecto | Revisar arranque y medio |
| No encuentra el disco | Falta controlador o modo de controladora no reconocido | Cargar controlador o revisar controladora |
| Error de lectura durante instalación | ISO corrupta o medio defectuoso | Verificar ISO y rehacer medio |
| Arranca pero no tiene red | Tarjeta no reconocida o interfaz incorrecta | Revisar interfaces/controlador/configuración |
| Dos equipos tienen la misma IP | IP ya utilizada o clonación sin cambiar configuración | Comprobar IP y reservar direcciones |
| Falla la actualización | Poco espacio o no hay acceso al repositorio | Liberar espacio y revisar red/DNS |
| Hora incorrecta | Zona horaria o sincronización incorrecta | Corregir zona y sincronización |

> **Idea clave:** el parte de incidencias se convierte en una base de conocimiento para futuros problemas.

### Consejo profesional

> **Documentar mientras se instala, no al terminar.**

Lo que se intenta reconstruir de memoria suele contener errores.

---

## 3.4. Diagnóstico guiado: el servidor está instalado pero no da servicio

### Situación

Servidor:

```text
192.0.2.10/24
```

Puerta de enlace:

```text
192.0.2.1
```

Cliente:

```text
192.0.2.50/24
```

### Método de diagnóstico

#### Paso 1. Configuración local

```bash
ip -br address
```

Comprobar que aparece:

```text
192.0.2.10/24
```

Si aparece una dirección:

```text
169.254.x.x
```

→ no se ha aplicado correctamente la configuración de red.

#### Paso 2. Puerta de enlace

```bash
ping -c 4 192.0.2.1
```

Si responde:

- la interfaz funciona;
- la configuración básica es coherente;
- existe conectividad con la red.

Si falla en una máquina virtual:

> **Primer sospechoso: modo de red incorrecto.**

#### Paso 3. Cliente → servidor

Desde el cliente:

```text
ping 192.0.2.10
```

Si falla:

- comprobar configuración del cliente;
- comprobar que ambos están en la misma red;
- revisar cortafuegos.

#### Paso 4. Servicio y cortafuegos

En el servidor:

```bash
ss -tulpn
```

Comprobar:

- ¿el servicio está arrancado?
- ¿está escuchando en el puerto esperado?
- ¿el cortafuegos permite el tráfico?

Dos situaciones típicas:

**No está escuchando**
→ arrancar y habilitar el servicio.

**Está escuchando pero no se puede acceder**
→ revisar el cortafuegos.

#### Paso 5. DNS

Cuando ya funciona la conexión por IP:

```powershell
nslookup srv-datos
```

Si falla:

- revisar DNS del cliente;
- comprobar que el nombre está registrado;
- comprobar que resuelve a la IP correcta.

> **Remarcar:** si funciona por IP pero no por nombre, **no es un problema de conectividad IP**; es un problema de resolución de nombres.

### Orden mental para diagnosticar

```text
¿Tiene IP?
   ↓
¿Llega a la puerta de enlace?
   ↓
¿El cliente llega por IP?
   ↓
¿El servicio escucha?
   ↓
¿El cortafuegos permite el puerto?
   ↓
¿Resuelve el nombre?
```

> **REGLA DE ORO:** avanzar de lo más básico a lo más específico. El primer fallo señala dónde buscar.

---

## 3.5. Servidores actuales: Windows Server, Linux y virtualización

### Tendencia actual

Los servidores actuales tienden a:

- Utilizar menos componentes innecesarios.
- Ejecutarse sin interfaz gráfica cuando no es necesaria.
- Administrarse remotamente.
- Ejecutarse sobre máquinas virtuales.
- Utilizar contenedores.
- Automatizar su despliegue.

### Windows Server

- Sistema propietario.
- Versiones con soporte a largo plazo.
- Integración con servicios Microsoft y nube.
- Puede utilizar **Server Core**, sin interfaz gráfica completa.

### Linux Server

Ejemplos:

- Ubuntu Server LTS.
- Debian.
- Rocky Linux.

Características:

- Software libre.
- Estabilidad.
- Amplio uso en servidores.
- Habitualmente puede instalarse sin entorno gráfico.

### ¿Por qué utilizar servidores sin GUI?

- Menor consumo de recursos.
- Menor cantidad de componentes.
- Menor superficie de ataque.
- Administración remota y por línea de comandos.

> **Remarcar:** en un servidor no se instala una interfaz gráfica porque "quede más bonito"; se instala si aporta una utilidad real.

### Formas de ejecutar servidores

| Modelo | Característica |
|---|---|
| Servidor físico | Modelo tradicional |
| Máquina virtual | Varios servidores sobre un mismo equipo |
| Contenedor | Aísla aplicaciones/servicios y facilita el despliegue |

> **Idea clave:** virtualización y contenedores permiten aprovechar mejor los recursos y facilitar el despliegue.

---

## 3.6. Aprovisionamiento automatizado y servidores en la nube

### Automatización

Configurar servidores manualmente:

- consume tiempo;
- puede provocar errores;
- dificulta repetir exactamente la configuración.

El **aprovisionamiento automatizado** permite describir la configuración deseada y aplicarla automáticamente.

Ejemplo:

**Ansible**

Puede automatizar:

- instalación de paquetes;
- configuración;
- servicios;
- usuarios;
- ajustes del sistema.

> **Idea clave:** describimos cómo queremos que quede el servidor y la herramienta aplica esa configuración de forma repetible.

### Ventajas

- Rapidez.
- Menos errores.
- Configuración repetible.
- Facilidad para reconstruir servidores.
- Escalabilidad.

### Nube: IaaS

**IaaS = Infraestructura como Servicio**

En lugar de comprar y mantener un servidor físico:

- se utiliza infraestructura de un proveedor cloud;
- se crea un servidor virtual;
- se asignan recursos;
- se paga según el servicio contratado;
- se puede aumentar o reducir la capacidad.

### Entornos híbridos

Una organización puede combinar:

- servidores propios;
- máquinas virtuales;
- servicios en la nube.

> **Remarcar:** la administración de servidores evoluciona hacia **automatización + virtualización + nube**.

---

# Resumen de la unidad

## Conceptos que deben quedar claros

1. **Conectividad:** comprobar de lo más básico a lo más específico.
2. **Diagnóstico:** el primer fallo indica dónde buscar.
3. **IP vs nombre:** si funciona por IP pero no por nombre, revisar DNS.
4. **Puertos:** comprobar que el servicio escucha y que el cortafuegos permite el tráfico.
5. **Virtualización:** permite crear laboratorios completos sobre un único equipo.
6. **NAT vs puente:** NAT facilita salida; puente permite que otros equipos accedan directamente al servidor.
7. **Instantáneas:** permiten volver atrás, pero no sustituyen a las copias de seguridad.
8. **Documentación:** registrar configuración, comprobaciones e incidencias.
9. **Parte de incidencias:** registrar síntoma, causa y solución.
10. **Servidores sin GUI:** reducen recursos y componentes innecesarios.
11. **Contenedores:** aíslan servicios y facilitan su despliegue.
12. **Automatización:** permite configurar servidores de forma rápida y repetible.
13. **IaaS:** proporciona servidores virtuales bajo demanda en la nube.

## Preguntas finales para la clase

- ¿En qué orden comprobarías un servidor al que no puede acceder un cliente?
- ¿Qué diferencia hay entre comprobar conectividad por IP y por nombre?
- Si funciona `ping` por IP pero no por nombre, ¿dónde buscarías?
- ¿Qué información proporciona `ss -tulpn`?
- ¿Qué diferencia hay entre un puerto cerrado y un servicio detenido?
- ¿Por qué NAT no suele ser adecuado para un servidor al que deben acceder otros equipos?
- ¿Qué ventaja tiene el modo puente?
- ¿Una instantánea es una copia de seguridad? ¿Por qué?
- ¿Qué información debe aparecer en una ficha de instalación?
- ¿Por qué no deben aparecer las contraseñas en la documentación?
- ¿Qué información debe contener un parte de incidencias?
- ¿Por qué conviene documentar mientras se instala?
- ¿Qué ventajas tiene instalar un servidor sin interfaz gráfica?
- ¿Qué diferencia hay entre una máquina virtual y un contenedor?
- ¿Qué problema intenta solucionar Ansible?
- ¿Qué significa IaaS?
- ¿Hacia dónde está evolucionando la administración de servidores?

## Idea final

> **Un servidor no está terminado cuando arranca: está terminado cuando funciona, se puede comprobar, se puede diagnosticar y se puede reconstruir.**

---------------
---------------

# Práctica

- Ping entre las dos máquina (Ubuntu y Windows Server)
- Puntos a tener en cuenta:
    - Requisitos de hardware para Windows Server https://learn.microsoft.com/es-es/windows-server/get-started/hardware-requirements?tabs=cpu&pivots=windows-server-2025
    - Requisitos de hardware para Ubuntu Server https://documentation.ubuntu.com/server/reference/installation/system-requirements/
    - Subir la memoria de tarjeta de vídeo
    - IP que viene por DHCP, poner el modo red en puente
    - Deshabilitar firewall

